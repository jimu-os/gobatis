<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sql SYSTEM "http://sgo.com">

<sql namespace="user">
    <select id="SelectStudent">

    </select>
    <select id="SelectStudent2">

    </select>

    <select id="select03">
        select * from student where sss={name} and
        <for slice="{arr}" item="obj" column="id" open="("  separator="," close=")" >
            {obj}
        </for>
        <if expr="1==1">
          and abc = 1
            <if expr="1==1">
                and 1=1
                <if expr="1!=1">
                    or 1!=1
                </if>
            </if>
           or cba=1
        </if>
        or
        name = {name} and 1=1
    </select>
</sql>